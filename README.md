# Sample-Unit-Contract-Test
Writing automated smart contract tests using chai assertions and hardhat

npm install --save-dev hardhat
npx hardhat
npx hardhat compile
npm install @openzeppelin/contracts
npm install --save-dev chai
